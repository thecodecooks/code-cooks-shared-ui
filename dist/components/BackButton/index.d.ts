import { ReactElement } from 'react';
interface BackButtonProps {
    className?: string;
    to: string;
}
export declare function BackButton({ className, to, }: BackButtonProps): ReactElement;
export {};
